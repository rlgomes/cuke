cuke
----

e2e testing framework built on cucumber that uses fuzzy matching to simplify and
harden the writing of web UI related tests.

install
-------

To build and run locally you should run `npm run build` and `npm link` to have
the binary `cuke` accessible.

For other packages requiring this one to build and use a specific version we
will add those instructions soon. ***TODO***
